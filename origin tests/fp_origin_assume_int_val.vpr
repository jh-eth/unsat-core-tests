// Value-origin false-positive test: assume exact.

function unused_pre_assume_int(x: Int): Int
  requires x > 0
{
  0
}

method caller()
{
  var x: Int
  assume x == 3
  assert unused_pre_assume_int(x) == 0
}
